# --- Phase 1: desktop shell, database, config, logging ---
PySide6==6.7.3
SQLAlchemy==2.0.35
python-dotenv==1.0.1
cryptography==43.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
APScheduler==3.10.4
keyring==25.4.1
tzdata==2024.2
pywin32==306; sys_platform == "win32"

# --- Phase 2: resume parsing ---
PyMuPDF==1.24.10
python-docx==1.1.2

# --- Phase 3: job source HTTP/networking ---
httpx==0.27.2
beautifulsoup4==4.12.3
tenacity==9.0.0

# Optional: browser fallbacks for the "any career page" job source
# (app/jobs/career_page_source.py) - used only when a plain HTTP GET
# doesn't find a job list. Two tiers share this one dependency: an
# invisible headless render (for JS-only pages) and, opt-in only,
# attaching to the user's own already-running Chrome/Edge over the
# Chrome DevTools Protocol (for sites that specifically block anonymous
# automated browsers). Genuinely optional: the package itself is small,
# but its browser binary (~150-300MB) is fetched separately via
# `playwright install chromium`, so it is NOT bundled into the
# PyInstaller build by default (see packaging/aijobfinder.spec and
# docs/AI_MODELS.md for the tradeoff and how to enable it). If this
# package or its browser isn't installed, both fallback tiers are
# skipped and the source still works from static HTML + the heuristic/AI
# extraction - it just won't see JS-only or bot-blocked listings.
playwright==1.47.0

# --- Phase 4: embeddings / ML matching ---
# torch is CPU-only - install from the PyTorch CPU index to avoid pulling
# in ~2GB of unused CUDA libraries (see README build instructions):
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
sentence-transformers==3.1.1
scikit-learn==1.5.2
numpy==2.1.1
openai==1.51.0
voyageai==0.2.3

# --- Phase 5: LLM providers ---
anthropic==0.34.2

# --- Testing ---
pytest==8.3.3
pytest-qt==4.4.0
